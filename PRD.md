# 欠款管理系统 PRD 文档

## 1. 产品概述

### 1.1 产品名称
欠款管理系统（QianKuanDemo）

### 1.2 产品定位
一款轻量级的个人/小微企业欠款管理工具，帮助用户记录和追踪客户欠款、还款情况，支持信用额度管理、账期设置和逾期催收提醒。

### 1.3 目标用户
- 小微企业主
- 个体经营者
- 需要管理客户账款的个人用户

---

## 2. 功能模块

### 2.1 首页（Dashboard）

#### 2.1.1 双视图模式
首页支持两种视图模式切换：

**异常提醒模式（催收模式）**
- **逾期总额看板**：显示所有逾期客户的累计逾期金额
- **逾期客户列表**：仅显示有逾期欠款的客户
- **单笔逾期展示**：
  - 显示客户名称、逾期天数、金额
  - 如有备注，显示备注内容（前14字符，可展开）
- **多笔逾期展示**：
  - 显示"共N笔逾期，点击查看明细"
  - 点击卡片展开/收起逾期明细列表
  - 明细中显示每笔逾期的日期、天数、金额、备注（备注可展开）
- **空状态**：无逾期时显示正向反馈"太棒了，目前没有逾期账单"

**收支明细模式（账目模式）**
- **统计卡片**：
  - 总待收：欠款 - 还款的差额
  - 总欠款：所有客户的累计欠款总额
  - 总还款：所有客户的累计还款总额
  - 今日欠款：今日新增欠款金额
  - 今日还款：今日新增还款金额
- **交易流水列表**：按时间倒序显示所有交易记录

**Tab切换**
- 点击Tab切换视图模式
- Tab数字徽章：
  - 异常提醒：显示逾期客户数量
  - 收支明细：显示今日交易数量
- 支持左右滑动手势切换（需真机支持）

#### 2.1.2 搜索与筛选
- **客户名称搜索**：支持模糊搜索客户名称
- **筛选面板**：
  - **交易类型筛选**：
    - 全部：显示所有交易记录
    - 欠款：仅显示欠款记录
    - 还款：仅显示还款记录
  - **交易日期筛选**：支持选择日期范围
  - **账单状态筛选**：
    - 全部：显示所有记录
    - 已逾期：仅显示已逾期的欠款记录
    - 3天内到期：仅显示3天内到期的欠款记录
  - **筛选互斥逻辑**：
    - 选择"还款"类型时，账单状态筛选自动禁用
    - 显示提示"还款记录不支持按账单状态筛选"
- **筛选结果数量**：实时显示筛选结果条数
- **清除筛选**：一键清除所有筛选条件

#### 2.1.3 交易流水卡片
- 每条记录显示：
  - 客户名称
  - 交易类型标签（欠款/还款）
  - 交易日期
  - 金额（欠款显示负数红色，还款显示正数绿色）
  - 备注（支持展开/收起，统一展示前14个字符，超过显示展开按钮）
- 点击卡片可编辑交易记录

### 2.2 客户管理

#### 2.2.1 客户列表
- 支持客户名称搜索
- 卡片式展示客户信息
- 按逾期状态排序（逾期客户优先显示）

#### 2.2.2 客户卡片信息
- **客户名称**：加粗显示
- **电话号码**：显示在名称右侧，小字体灰色
- **信用额度展示**：
  - 无限额度：显示"不限额度"（蓝色文字）
  - 有限额度：显示"剩余额度/信用额度"格式
    - 剩余金额：大号加粗字体
    - 总额度：灰色小字
- **进度条**：
  - 显示剩余额度百分比
  - 颜色规则：
    - 剩余 > 30%：绿色（emerald-500）
    - 剩余 10%-30%：琥珀色（amber-500）
    - 剩余 < 10%：玫红色（rose-500）
  - 信用额度为0时不显示进度条
- **固定账期**：显示账期天数
- **账期状态提醒**：
  - 已逾期：红色警告图标 + "已逾期 N 天"
  - 今日到期：橙色提示 "今日到期"
  - 即将到期（3天内）：黄色提醒图标 + "剩余 N 天"
  - 无欠款：不显示任何到期提醒

#### 2.2.3 添加/编辑客户
- **客户名称**：必填，1-20个字符
- **电话号码**：选填，11位手机号格式验证
- **信用额度**：
  - 默认为"无限额度"
  - 可切换为自定义额度
  - 自定义额度必须为 ≥ 0 的整数
  - 0 表示不允许产生欠款
- **固定账期**：默认30天，必须 > 0

### 2.3 交易管理

#### 2.3.1 录入欠款
- 选择客户
- 输入金额
- 选择日期（默认今天）
- 添加备注（选填）
- **信用额度校验**：
  - 信用额度为0时禁止录入欠款
  - 欠款金额超过剩余额度时警告并阻止提交
- **防重复提交**：提交中禁用按钮，显示"提交中..."

#### 2.3.2 录入还款
- 选择客户
- 输入金额
- 选择日期（默认今天）
- 添加备注（选填）
- 还款金额可以超过当前欠款（产生预付款/退款）
- **防重复提交**：提交中禁用按钮，显示"提交中..."

#### 2.3.3 编辑交易
- 点击交易卡片打开编辑抽屉
- **编辑欠款时信用额度计算**：排除当前交易，避免重复计算
- 可修改客户、金额、日期、备注
- **防重复提交**：提交中禁用按钮，显示"提交中..."

#### 2.3.4 删除交易
- 长按交易卡片触发删除确认（需真机支持）

#### 2.3.5 AI 催款助手

#### 功能概述
- **功能入口**：异常提醒模式下的逾期客户卡片底部
- **功能描述**：AI 根据客户逾期信息，自动生成催款文案
- **目标用户**：小微经营者面对熟人或老客户逾期时，不知道怎么优雅催款

#### 多笔逾期支持
- **信息展示**：显示逾期笔数、最长逾期天数、逾期总额
- **明细查看**：多笔逾期时可展开查看每笔明细（日期、逾期天数、金额、备注）
- **文案生成**：AI 生成文案时包含所有逾期记录信息

#### 文案风格
| 风格 | 描述 | 适用场景 |
|------|------|---------|
| 🌸 礼貌温和 | 适合首次催款、关系好的客户 | 维护良好关系 |
| 💼 专业正式 | 适合B2B客户、商务往来 | 保持专业形象 |
| 💭 委婉提醒 | 适合熟人、不好意思开口 | 降低心理负担 |
| 😄 幽默轻松 | 适合老客户、年轻客户 | 轻松化解尴尬 |

#### 使用流程
1. 在异常提醒模式下，点击逾期客户卡片底部的"AI催款"按钮
2. 在弹窗中查看逾期信息（多笔可展开查看明细）
3. 选择催款风格（大卡片手感设计）
4. 点击"生成文案"按钮（带 Shimmer 流光动效）
5. 查看生成的文案，一键复制到剪贴板
6. 可直接粘贴到微信发送给客户

#### 交互设计
- **按钮样式**：Ghost Button 样式（淡紫背景 + 紫色文字），不遮挡红色逾期预警
- **弹窗设计**：底部滑出抽屉，与记一笔弹窗保持一致
- **逾期明细**：多笔逾期时默认折叠，点击"查看逾期明细"展开
- **风格选择**：大卡片手感设计，移除传统单选圆圈，点击整行选中
- **选中态**：紫色边框 + 淡紫色投影 + 勾选图标
- **生成动效**：按钮流光效果模拟 AI 生成中状态

#### 技术实现
- 调用智谱 GLM-4-Flash API 生成文案
- 系统 Prompt 根据风格类型调整语气
- 传入客户名称、逾期笔数、每笔明细（金额、天数、备注）、总额作为上下文
- 生成文案限制在 200 字以内

#### 视觉规范
- 主按钮：`bg-gradient-to-r from-purple-500 to-pink-500`
- Ghost 按钮：`bg-purple-50 text-purple-600 border border-purple-200`
- 选中卡片投影：`shadow-[0_2px_12px_-2px_rgba(168,85,247,0.2)]`

### 2.4 认证模块

#### 2.4.1 用户注册
- 邮箱注册账号
- 填写邮箱、密码、确认密码
- 注册成功后发送验证邮件
- 自动登录

#### 2.4.2 用户登录
- 邮箱+密码登录
- 登录成功后自动同步云端数据

#### 2.4.3 退出登录
- 安全退出当前账号
- 清除本地会话状态

#### 2.4.4 修改密码
- 输入当前密码验证身份
- 输入新密码（至少6位）
- 确认新密码
- 密码修改成功后需重新登录

#### 2.4.5 修改邮箱
- 显示当前邮箱
- 输入新邮箱
- 输入当前密码确认身份
- 发送验证邮件到新邮箱

#### 2.4.6 忘记密码
- 输入注册邮箱
- 发送密码重置邮件
- 点击邮件链接重置密码

### 2.5 个人中心

#### 2.5.1 用户信息展示
- 显示当前登录用户邮箱
- 显示登录状态

#### 2.5.2 账户安全
- 修改邮箱入口
- 修改密码入口

#### 2.5.3 数据说明
- 登录后数据自动同步到云端
- 更换设备登录同一账号即可查看数据
- 数据安全存储在 Supabase 云端

### 2.6 PWA 功能

#### 2.6.1 应用安装
- 支持添加到主屏幕
- 独立应用窗口运行
- 自定义应用图标

#### 2.6.2 离线支持
- Service Worker 缓存静态资源
- 离线时可访问已缓存页面

#### 2.6.3 动态配置
- 回调地址动态获取当前域名
- 支持多环境部署（本地、Vercel）

---

## 3. 信用额度系统

### 3.1 额度定义
- **无限额度**：使用 -1 表示，UI显示为"不限额度"
- **有限额度**：≥ 0 的整数
  - 0：表示客户不允许产生欠款
  - > 0：表示客户的最大欠款额度

### 3.2 剩余额度计算
```
剩余额度 = 信用额度 - (累计欠款 - 累计还款)
```

### 3.3 额度控制规则
1. **无限额度客户**：不限制欠款金额
2. **额度为0的客户**：禁止录入任何欠款
3. **有限额度客户**：
   - 欠款金额 ≤ 剩余额度：允许录入
   - 欠款金额 > 剩余额度：禁止录入并提示警告

### 3.4 编辑欠款时的额度计算
- 编辑欠款时排除当前交易
- 剩余额度 = 信用额度 - (其他欠款 - 还款)
- 允许修改为不超过剩余额度+原金额的任意值

### 3.5 额度状态分级（动态预警）

#### 3.5.1 状态定义
| 状态 | 条件 | 说明 |
|------|------|------|
| 正常 | 剩余额度 > 30% | 额度充足 |
| 预警 | 剩余额度 10%-30% | 额度紧张 |
| 用尽 | 剩余额度 = 0 | 无法新增欠款 |
| 超额 | 剩余额度 < 0 | 已超出信用额度 |

#### 3.5.2 进度条颜色联动
| 状态 | 进度条颜色 | 文字颜色 |
|------|-----------|---------|
| 正常 | `bg-emerald-500` | `text-emerald-600` |
| 预警 | `bg-amber-500` | `text-amber-600` |
| 用尽/超额 | `bg-rose-500` | `text-rose-600` |

#### 3.5.3 客户列表排序优先级
1. 已超额（最高优先）
2. 已逾期
3. 额度用尽
4. 额度预警
5. 正常状态

### 3.6 超额状态处理

#### 3.6.1 超额客户限制
- 禁止新增欠款
- 客户卡片显示"已超额"标签
- 显示具体超出金额
- 引导用户引导客户先还款

#### 3.6.2 修改额度的二次确认
当用户将额度设置得低于当前欠款时：
- 弹出确认弹窗
- 提示内容："当前设置的额度 ¥xxx 低于已有欠款 ¥xxx，该客户将进入'超额'状态（超出 ¥xxx）并无法新增欠款，是否确认？"
- 编辑页面实时显示当前欠款和超额预警

### 3.7 新增欠款拦截提示
- 已超额客户：`录入失败！该客户已超出信用额度 ¥xxx，请先引导客户还款`
- 将导致超额：`录入失败！该笔金额将导致客户超出信用额度 ¥xxx`

---

## 4. 逾期计算系统

### 4.1 逾期判断逻辑
```
逾期天数 = 当前日期 - (交易日期 + 账期天数)
```
- 逾期天数 > 0：已逾期
- 逾期天数 = 0：今日到期
- 逾期天数 < 0：未到期

### 4.2 逾期金额计算
- 仅计算已逾期的欠款记录
- 已还款部分自动扣减
- 同一客户多笔逾期取最长逾期天数

### 4.3 逾期客户显示
- 按逾期天数降序排列
- 显示逾期总额和最长逾期天数
- 多笔逾期可展开查看明细

---

## 5. 技术架构

### 5.1 技术栈

#### 前端技术
| 技术 | 版本 | 用途 |
|-----|------|------|
| React | 18.x | UI 框架 |
| TypeScript | 5.x | 类型安全 |
| Vite | 8.x | 构建工具 |
| TailwindCSS | 4.x | 样式框架 |
| Zustand | 4.x | 状态管理 |
| Lucide React | 图标库 | 图标组件 |
| date-fns | 日期处理 | 日期格式化 |

#### 后端技术
| 技术 | 版本 | 用途 |
|-----|------|------|
| Supabase | 云服务 | 身份认证、数据库、RLS |
| PostgreSQL | 15+ | 关系型数据库 |

### 5.2 数据模型

#### Customer（客户）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 主键 |
| user_id | uuid | 用户ID（关联auth.users）|
| name | string | 客户名称 |
| phone | string | 电话号码 |
| credit_limit | number | 信用额度（-1表示无限） |
| payment_term | number | 固定账期（天） |
| created_at | timestamp | 创建时间 |

#### Transaction（交易）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 主键 |
| user_id | uuid | 用户ID（关联auth.users）|
| customer_id | number | 客户ID |
| type | enum | 交易类型（DEBT/PAYBACK） |
| amount | number | 金额 |
| note | string | 备注 |
| occurred_at | timestamp | 交易时间 |
| due_date | timestamp | 到期日期 |
| created_at | timestamp | 创建时间 |

### 5.3 用户隔离
- 使用 user_id 实现多用户数据隔离
- 每个用户只能看到自己的数据
- 通过 Supabase RLS（行级安全策略）实现

### 5.4 项目结构

```
qiankuandemo/
├── docs/                    # 文档目录
│   ├── CHANGELOG.md        # 更新日志
│   ├── SUPABASE_SETUP.md   # Supabase 配置指南
│   └── TEST_REPORT.md      # 测试报告
├── src/
│   ├── components/         # React 组件
│   │   ├── AddCustomerDialog.tsx    # 新增/编辑客户弹窗
│   │   ├── ChangeEmail.tsx          # 修改邮箱弹窗
│   │   ├── ChangePassword.tsx       # 修改密码弹窗
│   │   ├── CustomModal.tsx          # 自定义确认弹窗
│   │   ├── Toast.tsx                # 轻提示组件
│   │   └── TransactionDrawer.tsx    # 记一笔弹窗
│   ├── lib/                # 工具函数
│   │   ├── supabase.ts              # Supabase 客户端
│   │   ├── supabaseApi.ts           # API 接口封装
│   │   └── utils.ts                 # 通用工具函数
│   ├── pages/               # 页面组件
│   │   ├── Dashboard.tsx            # 首页（数据统计、交易列表）
│   │   ├── Customers.tsx            # 客户列表页
│   │   ├── Profile.tsx             # 个人中心页
│   │   ├── Login.tsx               # 登录组件
│   │   ├── Register.tsx            # 注册组件
│   │   └── ForgotPassword.tsx      # 忘记密码组件
│   ├── store/               # Zustand 状态管理
│   │   ├── authStore.ts            # 认证状态
│   │   ├── customerStore.ts        # 客户数据
│   │   ├── transactionStore.ts     # 交易数据
│   │   ├── modalStore.ts           # 弹窗状态
│   │   ├── toastStore.ts           # 轻提示状态
│   │   └── viewModeStore.ts       # 视图模式状态
│   ├── types/               # TypeScript 类型定义
│   │   └── index.ts
│   ├── App.tsx              # 根组件
│   ├── main.tsx             # 入口文件
│   └── index.css            # 全局样式
├── index.html
├── manifest.json            # PWA 配置
├── package.json
├── tsconfig.json
├── tailwind.config.js
├── vite.config.ts
├── PRD.md
└── README.md
```

---

## 6. 版本历史

### v1.2.4（当前版本）

#### Bug修复
- **修复逾期计算逻辑**：
  - 修复逾期计算没有考虑还款情况的问题
  - 添加欠款剩余金额计算逻辑
  - 用还款按时间顺序抵扣欠款
  - 只计算未还清且逾期的欠款
- **影响范围**：
  - 异常提醒数量统计
  - 客户列表逾期状态显示

### v1.2.3

#### 落地页优化
- **首屏视觉优化**：
  - 主标题改为渐变色大号"别让欠款拖垮你的生意"
  - 添加 `whitespace-nowrap` 确保一行展示
  - 移除 PM 标签，简化首屏
- **交互演示区域**：
  - 新增 AI 催款助手选择界面 + 微信对话模拟
  - 支持点击切换风格（温和/正式/幽默）
  - 右侧对话同步变化
- **痛点区块优化**：
  - 使用插画小人（圆形背景 + 组合 emoji）
  - 重点痛点高亮显示
- **页面布局调整**：
  - 页面顺序: Hero → 痛点 → 看看长什么样 → AI Demo → 功能
  - 看看长什么样改为2列布局
  - 移除重复的 AI 催款截图

### v1.2.2

#### 新增功能
- **客户档案页面视觉重塑**：
  - 新增客户头像（姓氏首字母 + 彩色渐变背景）
  - 卡片圆角从 `rounded-lg` 升级为 `rounded-2xl`
  - 金额层级重构：主金额突出显示，辅助信息小字
  - 底部信息左右布局（账期 + 逾期标签）
  - 逾期状态使用半透明背景 Pill Badge
- **我的页面优化**：
  - 头像缩小至 `w-10 h-10`，使用卡通默认头像 👤
  - 登录状态头像使用渐变背景
  - 数据存储文案字体缩小，间距紧凑

#### Bug修复
- 修复邮箱显示截断问题，改为 `break-all` 换行显示
- 修复刷新页面后数据不加载的问题（添加 `initialized` 状态检查）
- 修复修改密码错误提示为中文友好提示
- 添加网络请求重试机制，提高网络不稳定情况下的用户体验

#### UI优化
- 客户卡片点击缩放效果从 `scale-95` 改为 `scale-[0.98]`
- 进度条样式优化：上下留白 + 柔和背景

#### 技术优化
- 清理冗余代码和未使用的导入
- 修复 TypeScript `any` 类型问题
- 添加 `useCallback` 优化 `getCustomerName` 函数
- 修复 ESLint 警告

### v1.2.0

#### 新增功能
- **信用额度动态预警系统**：
  - 额度状态分级：正常、预警、用尽、超额
  - 进度条颜色联动：绿色(>30%)、琥珀色(10-30%)、玫红色(<10%或超额)
  - 客户列表智能排序：超额 > 逾期 > 用尽 > 预警 > 正常
  - 超额客户禁止新增欠款
  - 修改额度时的二次确认弹窗
  - 编辑页面实时显示当前欠款和超额预警
- **本地-生产环境同步规范**：建立完整的开发部署流程文档

#### Bug修复
- 修复异常提醒中单笔逾期备注显示使用 line-clamp 的问题，改为统一的字符截断

#### UI优化
- 客户卡片新增"已超额"标签和超出金额显示
- 新增欠款拦截提示文案优化，显示具体超出金额
- 编辑客户时显示当前欠款信息

#### 文档更新
- 新增 `docs/SYNC_GUIDE.md` 本地-生产环境同步规范
- 更新信用额度系统详细说明（PRD 3.5-3.7节）

### v1.1.0

#### 新增功能
- PWA 支持（添加到主屏幕、离线访问）
- 动态回调地址配置（支持多环境部署）
- AI催款助手支持多笔逾期：展示所有逾期明细、生成包含完整信息的催款文案

#### Bug修复
- 修复编辑欠款时信用额度计算错误的问题
- 修复连续点击提交按钮导致重复创建记录的问题

#### UI优化
- 统一底部弹窗视觉规范（记一笔、新增客户、AI催款助手等）
- 优化弹窗高度从 90vh 降至 85vh，减少底部空白
- 统一表单元素间距和按钮样式
- 添加视觉规范文档（PRD 8.5节）
- AI催款助手多笔逾期明细支持展开/收起

#### 技术优化
- 迁移到官方 Supabase 云服务
- 更新 Supabase API Key 格式（sb_publishable_xxx）
- 清理阿里云 Supabase 相关配置和文档

#### 测试结果
- 所有核心功能测试通过 (12/12)

### v1.0.0

#### 新增功能
- 用户认证模块（注册、登录、退出）
- 修改密码功能
- 修改邮箱功能
- 忘记密码功能
- 个人中心页面
- 登录检查机制

#### 代码优化
- 删除 Dexie 本地数据库
- 删除冗余文件
- 简化代码逻辑
- 清理未使用方法

#### Bug修复
- 修复数据库 RLS 策略配置

---

## 7. 未来规划

### 7.1 短期规划
- [ ] 数据导出功能（Excel/CSV）
- [ ] 催收提醒推送通知
- [ ] 客户分组管理

### 7.2 中期规划
- [ ] 数据统计报表
- [ ] 账单打印功能
- [ ] 多设备同步优化

### 7.3 长期规划
- [ ] 移动端App
- [ ] 微信小程序
- [ ] 企业版功能

---

## 8. 附录

### 8.1 错误处理
- 网络错误：控制台显示错误日志，支持重试
- 数据验证错误：显示具体错误信息
- 权限错误：引导用户重新登录

### 8.2 性能要求
- 页面加载时间 < 2秒
- 列表滚动流畅（60fps）
- 筛选操作响应及时

### 8.3 兼容性要求
- 支持 iOS Safari
- 支持 Android Chrome
- 支持桌面浏览器（Chrome、Firefox、Safari、Edge）

### 8.4 部署环境
- **生产环境**：https://qiankuandemo.vercel.app
- **数据库**：Supabase 官方云服务
- **版本控制**：GitHub

### 8.5 视觉规范
详细的交互视觉规范请参考 [DESIGN_SPEC.md](./docs/DESIGN_SPEC.md) 文档，包含：
- 颜色系统
- 字体规范
- 间距系统
- 弹框规范
- 表单规范
- 按钮规范
- AI 功能专属规范
- 动画规范等

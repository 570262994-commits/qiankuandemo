# 欠款管理系统 PRD 文档

## 1. 产品概述

### 1.1 产品名称
欠款管理系统（QianKuanDemo）

### 1.2 产品定位
一款轻量级的个人/小微企业欠款管理工具，帮助用户记录和追踪客户欠款、还款情况，支持信用额度管理和账期设置。

### 1.3 目标用户
- 小微企业主
- 个体经营者
- 需要管理客户账款的个人用户

---

## 2. 功能模块

### 2.1 首页（Dashboard）

#### 2.1.1 数据概览
- **总欠款金额**：显示所有客户的累计欠款总额
- **总还款金额**：显示所有客户的累计还款总额
- **未结清金额**：欠款 - 还款的差额

#### 2.1.2 搜索与筛选
- **客户名称搜索**：支持模糊搜索客户名称
- **日期范围筛选**：支持选择开始日期和结束日期
- **交易类型筛选**：
  - 全部：显示所有交易记录
  - 欠款：仅显示欠款记录
  - 还款：仅显示还款记录

#### 2.1.3 交易流水列表
- 按时间倒序显示所有交易记录
- 每条记录显示：
  - 客户名称
  - 交易类型（欠款/还款）
  - 金额
  - 交易时间
  - 备注（支持展开/收起）

### 2.2 客户管理

#### 2.2.1 客户列表
- 支持客户名称搜索
- 卡片式展示客户信息

#### 2.2.2 客户卡片信息
- **客户名称**：加粗显示
- **电话号码**：显示在名称右侧，小字体灰色
- **信用额度展示**：
  - 无限额度：显示"不限额度"（蓝色文字）
  - 有限额度：显示"剩余额度/信用额度"格式
    - 剩余金额：大号加粗字体
    - 总额度：灰色小字
- **进度条**：
  - 显示剩余额度百分比
  - 颜色规则：
    - 剩余 > 30%：绿色（emerald-500）
    - 剩余 10%-30%：琥珀色（amber-500）
    - 剩余 < 10%：玫红色（rose-500）
  - 信用额度为0时不显示进度条
- **固定账期**：显示账期天数

#### 2.2.3 添加/编辑客户
- **客户名称**：必填
- **电话号码**：选填
- **信用额度**：
  - 默认为"无限额度"
  - 可切换为自定义额度
  - 自定义额度必须为 ≥ 0 的整数
  - 0 表示不允许产生欠款
- **固定账期**：默认30天，可自定义

### 2.3 交易管理

#### 2.3.1 录入欠款
- 选择客户
- 输入金额
- 选择日期
- 添加备注（选填）
- **信用额度校验**：
  - 信用额度为0时禁止录入欠款
  - 欠款金额超过剩余额度时警告并阻止提交

#### 2.3.2 录入还款
- 选择客户
- 输入金额
- 选择日期
- 添加备注（选填）

#### 2.3.3 交易记录管理
- 支持编辑交易记录
- 支持删除交易记录（需确认）

---

## 3. 信用额度系统

### 3.1 额度定义
- **无限额度**：使用 -1 表示，UI显示为"不限额度"
- **有限额度**：≥ 0 的整数
  - 0：表示客户不允许产生欠款
  - > 0：表示客户的最大欠款额度

### 3.2 剩余额度计算
```
剩余额度 = 信用额度 - (累计欠款 - 累计还款)
```

### 3.3 额度控制规则
1. **无限额度客户**：不限制欠款金额
2. **额度为0的客户**：禁止录入任何欠款
3. **有限额度客户**：
   - 欠款金额 ≤ 剩余额度：允许录入
   - 欠款金额 > 剩余额度：禁止录入并提示警告

---

## 4. 技术架构

### 4.1 技术栈
- **前端框架**：React 18 + TypeScript
- **构建工具**：Vite
- **样式方案**：Tailwind CSS v4
- **状态管理**：Zustand
- **后端服务**：Supabase（PostgreSQL + Auth + Realtime）
- **UI组件**：Lucide Icons

### 4.2 数据模型

#### Customer（客户）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 主键 |
| name | string | 客户名称 |
| phone | string | 电话号码 |
| credit_limit | number | 信用额度（-1表示无限） |
| payment_term | number | 固定账期（天） |
| device_id | string | 设备ID |
| created_at | timestamp | 创建时间 |

#### Transaction（交易）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 主键 |
| customer_id | number | 客户ID |
| type | enum | 交易类型（debt/payback） |
| amount | number | 金额 |
| note | string | 备注 |
| occurred_at | timestamp | 交易时间 |
| device_id | string | 设备ID |
| created_at | timestamp | 创建时间 |

### 4.3 设备隔离
- 使用 device_id 实现多设备数据隔离
- 每个设备只能看到自己的数据

---

## 5. 版本历史

### v0.3.0（当前版本）
- 优化客户卡片UI布局
- 重构信用额度展示方式
- 添加剩余额度进度条
- 实现信用额度校验机制
- 修复NaN计算错误

### v0.2.x
- 添加首页搜索和筛选功能
- UI优化和Toast提示
- 统一弹窗交互体验
- 添加备注展开/收起功能

### v0.1.x
- 基础客户管理功能
- 基础交易记录功能
- 数据持久化

---

## 6. 未来规划

### 6.1 短期规划
- [ ] 数据导出功能（Excel/CSV）
- [ ] 账单提醒功能
- [ ] 客户分组管理

### 6.2 中期规划
- [ ] 多用户支持
- [ ] 数据同步功能
- [ ] 报表统计功能

### 6.3 长期规划
- [ ] 移动端App
- [ ] 微信小程序
- [ ] 企业版功能

---

## 7. 附录

### 7.1 错误处理
- 网络错误：显示友好提示，支持重试
- 数据验证错误：显示具体错误信息
- 权限错误：引导用户重新登录

### 7.2 性能要求
- 页面加载时间 < 2秒
- 列表滚动流畅（60fps）
- 支持离线浏览（缓存策略）

## PWA 部署实施计划

### 第一阶段：PWA 基础配置
1. **创建 `public/manifest.json`** - PWA 配置文件
   - 应用名称：欠款助手
   - 主题色：#3b82f6
   - 图标配置

2. **创建 `public/sw.js`** - Service Worker
   - 离线缓存支持
   - 静态资源缓存

3. **修改 `index.html`** - 添加 PWA 标签
   - manifest 链接
   - iOS 沉浸式状态栏 meta 标签
   - apple-touch-icon

4. **创建应用图标**
   - `public/icons/icon-192.png`
   - `public/icons/icon-512.png`

5. **修改 `src/main.tsx`** - 注册 Service Worker

### 第二阶段：动态回调逻辑
6. **修改 `src/lib/supabase.ts`** - 动态获取当前域名
7. **修改 `src/store/authStore.ts`** - 确保 redirectTo 使用动态地址

### 第三阶段：iOS 安装引导
8. **创建 `src/components/InstallPrompt.tsx`** - iOS 安装引导组件
9. **修改 `src/pages/Dashboard.tsx`** - 集成引导组件

### 第四阶段：部署
10. 部署到 Vercel
11. 配置 Supabase 回调地址

---

**预计文件变更**：
- 新建 4 个文件
- 修改 5 个文件